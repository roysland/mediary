# Service worker push notifications
In settings, enable push notifications. 

# Ensure app is not in webview
Passkeys don't work in most webviews, at least not ios. We need to show a clear banner that this only works in browser.
Also, an install app banner should appear. If dismissed or clicked, never show again. 
