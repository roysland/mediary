Opening context menu on an entry and adding a trackable doesn't update the view. 
The trackable dialog on entries doesn't update the <output> value.


Uploading image doesn't work. Get invalid entry ID. There shouldn't be an "upload image" button, just a file picker. If the file picker has an image, add the attachment. 
Similarly, there should be an "upload image" option in the entry context menu

Both locally and in production, registering a trackable happens way to fast. We need to add an artificial delay. 

Shared report doesn't include any entries. 
Creating a shared report should make the "To date" the current date.